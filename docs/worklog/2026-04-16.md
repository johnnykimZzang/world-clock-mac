# 2026-04-16 작업 로그

## 1. Athens (그리스) 도시 추가

- **변경 목적**: 요청에 의해 Athens / Europe/Athens / EET 추가
- **수정 파일**: `src/lib/cities.ts`
- **문서 반영**: `docs/features.md` — 지원 도시 표에 Athens 추가

---

## 2. 트레이 컨텍스트 메뉴 + 업데이트 확인 기능

- **변경 목적**: 우클릭 메뉴로 버전 확인 및 수동 업데이트 체크 제공 + capabilities 권한 버그픽스
- **수정 파일**:
  - `src-tauri/src/lib.rs` — 트레이 우클릭 메뉴, `app.on_menu_event()`, `Emitter` trait
  - `src-tauri/capabilities/default.json` — `updater:default`, `process:allow-restart` 권한 추가, `"popover"` 윈도우 추가
  - `src/hooks/useUpdater.ts` — `listen("menu:check-updates")`, `upToDate` 상태, 3초 토스트
  - `src/App.tsx` — `upToDate` 토스트 UI
  - `src/components/Settings.tsx` — 하단 버전 표시, `getVersion()` 호출
- **문서 반영**:
  - `docs/features.md` — 트레이 컨텍스트 메뉴 섹션, 설정 화면 버전 표시 섹션 추가
  - `docs/architecture.md` — 섹션 1 "트레이 컨텍스트 메뉴" 추가, capabilities 권한 설명, hooks 설명 업데이트
  - `docs/release.md` — Gatekeeper 차단 트러블슈팅 추가, 업데이트 미감지 원인에 capabilities 항목 추가
- **남은 이슈**: Apple Developer 코드사인 없음 — 배포 빌드 설치 시 Gatekeeper 우회 필요
